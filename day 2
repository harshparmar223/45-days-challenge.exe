#include <stdio.h>

int main() {
  int i,number;
  
  printf("enter a number");
  scanf("%d",&number);
  
  for(i==1; i<=number; i++) 
  {
      
      
      printf("the value of i is %d\t",i);
  }

    return 0;
}
